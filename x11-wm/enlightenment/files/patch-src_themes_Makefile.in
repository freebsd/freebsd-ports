--- src/themes/Makefile.in.orig	Tue Jan  9 18:52:34 2001
+++ src/themes/Makefile.in	Tue Jan  9 18:55:07 2001
@@ -115,7 +115,7 @@
 @FSSTD_TRUE@ethemedir = $(prefix)/share/enlightenment/themes
 @FSSTD_FALSE@ethemedir = $(prefix)/enlightenment/themes
 
-EXTRA_DIST = BrushedMetal-Tigert.etheme ShinyMetal.etheme BlueSteel.etheme Ganymede.etheme  BrushedMetal-Tigert.etheme.ja ShinyMetal.etheme.ja BlueSteel.etheme.ja Ganymede.etheme.ja
+EXTRA_DIST = Marble.etheme BrushedMetal-Tigert.etheme ShinyMetal.etheme BlueSteel.etheme Ganymede.etheme  BrushedMetal-Tigert.etheme.ja ShinyMetal.etheme.ja BlueSteel.etheme.ja Ganymede.etheme.ja BrushedMetal-Tigert.etheme.zh_TW.Big5 ShinyMetal.etheme.zh_TW.Big5 BlueSteel.etheme.zh_TW.Big5
 mkinstalldirs = $(SHELL) $(top_srcdir)/mkinstalldirs
 CONFIG_HEADER = ../../econfig.h
 CONFIG_CLEAN_FILES = 
@@ -321,6 +321,7 @@
 
 install-data-local:
 	$(mkinstalldirs) $(DESTDIR)$(ethemedir)
+	gzip -d -c < $(srcdir)/Marble.etheme | (cd $(DESTDIR)$(ethemedir); rm -rf Marble; mkdir Marble; cd Marble; tar -xmf -)
 	gzip -d -c < $(srcdir)/BrushedMetal-Tigert.etheme | (cd $(DESTDIR)$(ethemedir); rm -rf BrushedMetal-Tigert; mkdir BrushedMetal-Tigert; cd BrushedMetal-Tigert; tar -xmf -)
 	gzip -d -c < $(srcdir)/ShinyMetal.etheme | (cd $(DESTDIR)$(ethemedir); rm -rf ShinyMetal; mkdir ShinyMetal; cd ShinyMetal; tar -xmf -)
 	gzip -d -c < $(srcdir)/BlueSteel.etheme | (cd $(DESTDIR)$(ethemedir); rm -rf BlueSteel; mkdir BlueSteel; cd BlueSteel; tar -xmf -)
@@ -331,6 +332,11 @@
 	gzip -d -c < $(srcdir)/ShinyMetal.etheme.ja | (cd $(DESTDIR)$(ethemedir)/ShinyMetal; tar -xmf -)
 	gzip -d -c < $(srcdir)/BlueSteel.etheme.ja | (cd $(DESTDIR)$(ethemedir)/BlueSteel; tar -xmf -)
 	gzip -d -c < $(srcdir)/Ganymede.etheme.ja | (cd $(DESTDIR)$(ethemedir)/Ganymede; tar -xmf -)
+
+# Taiwanese
+	gzip -d -c < $(srcdir)/BrushedMetal-Tigert.etheme.zh_TW.Big5 | (cd $(DESTDIR)$(ethemedir)/BrushedMetal-Tigert; tar -xmf -)
+	gzip -d -c < $(srcdir)/ShinyMetal.etheme.zh_TW.Big5 | (cd $(DESTDIR)$(ethemedir)/ShinyMetal; tar -xmf -)
+	gzip -d -c < $(srcdir)/BlueSteel.etheme.zh_TW.Big5 | (cd $(DESTDIR)$(ethemedir)/BlueSteel; tar -xmf -)
 
 	if [ -n "$(USER)" ]; then chown -R $(USER) $(DESTDIR)$(ethemedir); else chown -R root $(DESTDIR)$(ethemedir); fi
