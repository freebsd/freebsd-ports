--- Makefile.orig	Wed Sep 10 23:04:51 1997
+++ Makefile	Sun Dec 28 02:03:35 1997
@@ -1,11 +1,11 @@
 
-PREFIX      = /usr/local
+PREFIX      = $(X11BASE)
 BINDIR      = $(PREFIX)/bin
 LIBDIR      = $(PREFIX)/lib/X11/icewm
-ETCDIR      = /etc/X11/icewm
+ETCDIR      = ${LIBDIR}
 
-INSTALL     = install
-INSTALLDIR  = $(INSTALL) -m 755 -d
+INSTALL     = install -c
+INSTALLDIR  = mkdir -p
 INSTALLBIN  = $(INSTALL) -m 555
 INSTALLLIB  = $(INSTALL) -m 444
 #INSTALLETC  = $(INSTALL) -m 644
@@ -17,7 +17,7 @@
 
 all:
 	(cd doc ; make)
-	(cd src ; make LIBDIR=$(LIBDIR) ETCDIR=$(ETCDIR))
+	(cd src ; make LIBDIR=$(LIBDIR) ETCDIR=$(ETCDIR) PREFIX=$(PREFIX) )
 
 clean:
 	(cd doc ; make clean)
