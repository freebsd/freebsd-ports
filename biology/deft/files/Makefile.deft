#
# Makefile for DeFT
#
TARGET = DeFT.exec
FFLAGS = -O2 -malign-double -Nn802
LFLAGS = 
SRCS:sh = ls *.f
OBJS = $(SRCS:.f=.o)

all: ${TARGET}

${TARGET}: ${OBJS}
	${FC} ${LFLAGS} ${OBJS} -o $@

clean:
	rm -f ${OBJS} ${TARGET}

