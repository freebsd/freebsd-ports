#
# Makefile for DeFT
#
TARGET = DeFT.exec
.if ${MACHINE_ARCH} != "i386"
FFLAGS = -O2
.else
FFLAGS = -O2 -malign-double -Nn802
.endif
LFLAGS = 
SRCS:sh = ls *.f
OBJS = $(SRCS:.f=.o)

all: ${TARGET}

${TARGET}: ${OBJS}
	${FC} ${LFLAGS} ${OBJS} -o $@

clean:
	rm -f ${OBJS} ${TARGET}

