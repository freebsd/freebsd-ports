PROG=	wmbday
SRCS=	date.c main.c ring.c

PREFIX?=  ${X11BASE}

BINDIR=   ${PREFIX}/bin
SHAREDIR= ${PREFIX}
CFLAGS+=  -I${X11BASE}/include
LDFLAGS+= -L${X11BASE}/lib
LDADD=    -lX11 -lXext -lXpm

.include <bsd.prog.mk>
