--- configure.ac.orig	2026-09-20 00:33:22 UTC
+++ configure.ac
@@ -126,7 +126,7 @@ if test "x$with_anthy" = xyes; then
   ANTHY_LIBS="-lanthy -lanthydic"
   ANTHY_CFLAGS=""
   PKG_CHECK_EXISTS(
-    anthy >= 8622,
+    anthy >= 1.0,
     [AC_DEFINE(LIBANTHY_UTF8_CAPABLE, 1, "libanthy can handle UTF-8")],
     [PKG_CHECK_EXISTS(
        anthy < 1000, # https://salsa.debian.org/debian/anthy supports UTF-8.
@@ -154,7 +154,7 @@ if test "x$with_anthy_utf8" != xno; then
      [libanthy-unicode can handle UTF-8])],
     [PKG_CHECK_MODULES(
        ANTHY_UTF8,
-       anthy >= 8622,
+       anthy >= 1.0,
        [with_anthy_utf8=yes
         AC_DEFINE(LIBANTHY_UTF8_CAPABLE, 1, [libanthy can handle UTF-8])],
        [PKG_CHECK_MODULES(
