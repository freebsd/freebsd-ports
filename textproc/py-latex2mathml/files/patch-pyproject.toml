--- pyproject.toml.orig	1970-01-01 00:00:00 UTC
+++ pyproject.toml
@@ -77,5 +77,5 @@ module-root = ""
 module-root = ""
 
 [build-system]
-requires = ["uv_build>=0.10.11,<0.13.0"]
-build-backend = "uv_build"
+requires = ["flit-core >= 3.12"]
+build-backend = "flit_core.buildapi"
