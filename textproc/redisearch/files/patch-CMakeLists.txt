--- CMakeLists.txt.orig	2026-07-28 02:11:19 UTC
+++ CMakeLists.txt
@@ -14,8 +14,10 @@ if(APPLE)
 # Platform detection
 if(APPLE)
     set(OS "macos")
-elseif(UNIX)
-    set(OS "linux")
+elseif(CMAKE_SYSTEM_NAME STREQUAL "FreeBSD")
+    set(OS "freebsd")
+# elseif(UNIX)
+#    set(OS "linux")
 endif()
 message(STATUS "OS detected: ${OS}")
 
