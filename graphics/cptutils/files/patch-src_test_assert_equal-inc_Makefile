--- src/test/assert/equal-inc/Makefile.orig	2024-03-27 12:42:33 UTC
+++ src/test/assert/equal-inc/Makefile
@@ -16,7 +16,7 @@ install:
 veryclean: clean uninstall
 
 install:
-	$(INSTALL_PROGRAM) $(PROG) $(BINDIR)/$(PROG)
+	$(INSTALL_SCRIPT) $(PROG) $(BINDIR)/$(PROG)
 
 uninstall:
 	$(RM) $(BINDIR)/$(PROG)
