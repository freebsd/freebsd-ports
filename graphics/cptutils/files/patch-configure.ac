--- configure.ac.orig	2020-07-08 21:29:57 UTC
+++ configure.ac
@@ -117,11 +117,6 @@ if test $opt_enable_tests = yes; then
       AC_SUBST(WITH_ACCEPT, 1)
    fi
 
-   AC_PATH_PROG(XML2, xml2, no)
-   if test "$XML2" = no ; then
-      AC_MSG_ERROR(need xml2 program)
-   fi
-
    dnl The name "xmlstarlet" is used by Debian and derivatives since
    dnl presumably, the upstream name of "xml" is already taken or is
    dnl too generic -- we check for the formar first
