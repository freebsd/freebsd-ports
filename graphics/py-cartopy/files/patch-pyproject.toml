--- pyproject.toml.orig	2026-09-17 02:39:05 UTC
+++ pyproject.toml
@@ -1,6 +1,6 @@ requires = [
 [build-system]
 requires = [
-    "setuptools >= 77.0.3",
+    "setuptools >= 61",
     "Cython >= 3.0",
     # numpy requirement for wheel builds for distribution on PyPI - building
     # against 2.x yields wheels that are also compatible with numpy 1.x at
@@ -14,7 +14,7 @@ build-backend = "setuptools.build_meta"
 build-backend = "setuptools.build_meta"
 
 [project]
-name = "Cartopy"
+name = "cartopy"
 authors = [
     {name = "UK Met Office"},
 ]
@@ -22,8 +22,7 @@ keywords = ["cartography", "map", "transform", "projec
 readme = "README.md"
 requires-python = ">=3.11"
 keywords = ["cartography", "map", "transform", "projection", "pyproj", "shapely", "shapefile"]
-license = "BSD-3-Clause"
-license-files = ["LICENSE"]
+license = {text = "BSD-3-Clause"}
 classifiers = [
     'Development Status :: 4 - Beta',
     'Framework :: Matplotlib',
