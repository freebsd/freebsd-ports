--- Makefile.orig	Mon Mar 19 16:46:24 2001
+++ Makefile	Thu Dec 27 09:03:00 2001
@@ -1,10 +1,14 @@
-CC = arm-linux-gcc
-CFLAGS =  -O2 -L/skiff/local/arm-linux/lib/X11 -lX11 -lXpm
-PROGS = miniscrib
+#
+# $FreeBSD$
+#
 
-all: $(PROGS)
+PROG=miniscrib
+SRCS=miniscrib.c
+NOMAN=
 
-distclean clean:
-	rm -f $(PROGS)
+CFLAGS += -g -I${X11BASE}/include
+LDFLAGS+= -lX11 -lXpm -L${X11BASE}/lib
 
+BINDIR=${PREFIX}/bin
 
+.include <bsd.prog.mk>
