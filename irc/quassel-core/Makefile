MASTERDIR=	${.CURDIR}/../quassel

PKGNAMESUFFIX=	-core

COMMENT=	Qt 6 based distributed IRC client (core/server only)

OPTIONS_EXCLUDE=CLIENT KDE MONO SPELL NLS
OPTIONS_SLAVE=	CORE

.include "${MASTERDIR}/Makefile"
