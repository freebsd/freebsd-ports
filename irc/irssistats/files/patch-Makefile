$FreeBSD$

--- Makefile.orig	Sun Dec 14 10:40:10 2003
+++ Makefile	Sun Mar 14 22:17:20 2004
@@ -1,19 +1,25 @@
 PRE = /usr/local
 BIN = $(PRE)/bin
+SMP = $(PRE)/share/irssistats
 DAT = $(PRE)/share/irssistats/data
-DOC = $(PRE)/doc/irssistats
+DOC = $(PRE)/share/doc/irssistats
+CC ?= gcc
 
-irssistats:irssistats.c
-	gcc -o irssistats irssistats.c
+all:irssistats.c
+	${CC} ${CFLAGS} -o irssistats irssistats.c
 
 clean:
 	rm -f irssistats 
 
 install:irssistats
-	mkdir -p $(BIN) $(DAT) $(DOC)
+	mkdir -p $(BIN) $(DAT)
 	cp -f irssistats $(BIN)
+	cp -f sample.nickfile sample.configfile $(SMP)
 	cp -f data/* $(DAT)
-	cp -f COPYING README sample.nickfile sample.configfile $(DOC)
+.ifndef NOPORTDOCS
+	mkdir -p $(DOC)
+	cp -f COPYING README $(DOC)
+.endif
 
 uninstall:
 	rm -f $(BIN)/irssistats
