# $Id$
#

    SUBDIR += bitchx
    SUBDIR += blackened
    SUBDIR += bnc
    SUBDIR += ezbounce
    SUBDIR += irc
    SUBDIR += ircII
    SUBDIR += ircd-hybrid
    SUBDIR += ircii-epic
    SUBDIR += irssi
    SUBDIR += kvirc
    SUBDIR += muh
    SUBDIR += ninja
    SUBDIR += quirc
    SUBDIR += roxirc
    SUBDIR += sic
    SUBDIR += sirc
    SUBDIR += tkirc
    SUBDIR += trickyirc
    SUBDIR += xchat
    SUBDIR += xgirc
    SUBDIR += yagirc
    SUBDIR += zircon

.include <bsd.port.subdir.mk>
