https://github.com/OpenFodder/openfodder/issues/90
https://github.com/OpenFodder/openfodder/commit/79eccbfbca05

--- CMakeLists.txt.orig	2024-05-30 18:32:05 UTC
+++ CMakeLists.txt
@@ -17,6 +17,7 @@ find_package(SDL2Mixer REQUIRED)
 
 find_package(SDL2 REQUIRED)
 find_package(SDL2Mixer REQUIRED)
+find_package(Threads REQUIRED)
 
 # Locate git binary to provide information to the build environment
 find_package(Git)
@@ -46,4 +47,4 @@ target_include_directories(openfodder PRIVATE
     Source
     ${CMAKE_CURRENT_BINARY_DIR}/Source)
 
-target_link_libraries(openfodder SDL2::SDL2 SDL2_mixer::SDL2_mixer)
+target_link_libraries(openfodder SDL2::SDL2 SDL2_mixer::SDL2_mixer Threads::Threads)
--- Makefile.orig	2024-05-30 18:32:05 UTC
+++ Makefile
@@ -4,7 +4,7 @@ CC = clang++ $(CXXFLAGS) -c -Wall -std=c++14 -ferror-l
 DLibs = `sdl2-config --libs` -L/usr/local/lib  -lSDL2_mixer
 
 CC = clang++ $(CXXFLAGS) -c -Wall -std=c++14 -ferror-limit=100 $(pathInc) $(Libs)
-LD = clang++ $(LDFLAGS) obj/*.o $(DLibs)
+LD = clang++ $(LDFLAGS) obj/*.o -lpthread $(DLibs) 
 
 all : fodder
 
