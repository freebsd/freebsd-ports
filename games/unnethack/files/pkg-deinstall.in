#!/bin/sh
# $FreeBSD$

if [ "$2" = "POST-DEINSTALL" -a -d "%%DATADIR%%" ]; then
	echo ""
	echo "    If unnethack is being removed permanently, please remove"
	echo ""
        echo "        %%DATADIR%%"
	echo ""
fi
