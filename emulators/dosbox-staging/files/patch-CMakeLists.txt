--- CMakeLists.txt.orig	2026-08-26 12:24:41 UTC
+++ CMakeLists.txt
@@ -165,7 +165,7 @@ elseif (APPLE)
   set(DOSBOX_PLATFORM_MACOS ON)
   set(MACOSX ON)
 
-elseif (LINUX)
+elseif (UNIX)
   set(DOSBOX_PLATFORM_LINUX ON)
 
 else()
@@ -280,8 +280,7 @@ option(OPT_FORCE_DYNREC "Force DYNREC dynamic recompil
 
 option(OPT_FORCE_DYNREC "Force DYNREC dynamic recompiler (not x86-64 optimized)" OFF)
 
-if (CMAKE_SYSTEM_PROCESSOR  STREQUAL "AMD64"  OR
-    CMAKE_SYSTEM_PROCESSOR  STREQUAL "x86_64" OR
+if (CMAKE_SYSTEM_PROCESSOR  MATCHES "i386|amd64|x86_64"  OR
     CMAKE_OSX_ARCHITECTURES STREQUAL "x86_64")
 
   set(C_TARGET_CPU_X86    ON)
@@ -462,7 +461,7 @@ find_package(OpenGL REQUIRED)
 find_package(iir REQUIRED)
 find_package(PNG REQUIRED)
 find_package(OpenGL REQUIRED)
-find_package(MT32Emu REQUIRED)
+find_package(MT32Emu)
 find_package(FluidSynth REQUIRED CONFIG)
 if (C_ALSA)
   find_package(ALSA REQUIRED)
