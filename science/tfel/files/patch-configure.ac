--- configure.ac.orig	2016-09-22 08:21:19 UTC
+++ configure.ac
@@ -70,6 +70,9 @@ case $host in
 	TFEL_ARCH32="yes"
         ;;
 # machine amd opteron, 64 bits
+    amd64*bsd*)
+	TFEL_ARCH64="yes"
+        ;;
     x86_64*bsd*)
 	TFEL_ARCH64="yes"
         ;;
