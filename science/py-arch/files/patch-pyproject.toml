--- pyproject.toml.orig	2025-10-21 08:34:10 UTC
+++ pyproject.toml
@@ -3,8 +3,8 @@ requires = [
 requires = [
   'meson-python',
   "cython>=3.0.13,<4",  # Sync with CYTHON_MIN_VER in setup
-  "numpy>=2.0.0,<3",
-  "setuptools_scm>=9.2.0,<10",
+  "numpy",
+  "setuptools_scm",
 ]
 
 [project]
