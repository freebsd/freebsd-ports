;;
;; @(#)flim-setupel.el -- flim emacs load-path setup elisp file.
;;                                            --  by shige@FreeBSD.ORG

(setq load-path (append '(
	"@@PREFIX@@/@@EMACS_LIBDIR@@/site-lisp/flim"
 ) load-path))
(provide 'flim-setupel)
