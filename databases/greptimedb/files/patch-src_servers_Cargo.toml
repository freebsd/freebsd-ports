--- src/servers/Cargo.toml.orig	2026-09-16 03:17:27 UTC
+++ src/servers/Cargo.toml
@@ -145,7 +145,7 @@ zstd.workspace = true
 vrl.workspace = true
 zstd.workspace = true
 
-[target.'cfg(not(windows))'.dependencies]
+[target.'cfg(not(any(windows, target_os = "freebsd")))'.dependencies]
 tikv-jemalloc-ctl = { version = "0.7", features = ["use_std", "stats"] }
 
 [dev-dependencies]
