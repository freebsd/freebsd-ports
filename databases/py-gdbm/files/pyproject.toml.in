[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "gdbm"
version = "%%DISTVERSION%%"
description = "GNU dbm extension to Python"

[tool.setuptools]
packages = []
