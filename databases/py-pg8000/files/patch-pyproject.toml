--- pyproject.toml.orig	2024-01-14 09:52:53 UTC
+++ pyproject.toml
@@ -1,6 +1,6 @@ requires = [
 [build-system]
 requires = [
-    "setuptools >= 65",
+    "setuptools >= 63",
     "versioningit >= 2.1.0",
 ]
 build-backend = "setuptools.build_meta"
