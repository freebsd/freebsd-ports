--- pyproject.toml.orig	2026-08-28 09:11:54 UTC
+++ pyproject.toml
@@ -1,3 +1,3 @@
 [build-system]
-requires = ["pbr>=6.0.0", "setuptools>=64"]
+requires = ["pbr>=6.0.0", "setuptools>=61"]
 build-backend = "pbr.build"
