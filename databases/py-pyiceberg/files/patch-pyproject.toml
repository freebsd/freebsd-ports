-- Adjust pyproject metadata for the FreeBSD ports setuptools toolchain.

--- pyproject.toml.orig	2026-09-20 01:25:18 UTC
+++ pyproject.toml
@@ -21,8 +21,6 @@
 authors = [{ name = "Apache Software Foundation", email = "dev@iceberg.apache.org" }]
 requires-python = ">=3.10.0,<4.0.0"
 readme = "README.md"
-license = "Apache-2.0"
-license-files = ["LICENSE", "NOTICE"]
 classifiers = [
     "Operating System :: OS Independent",
     "Programming Language :: Python :: 3.10",
@@ -146,7 +144,7 @@ default-groups = [
 ]
 
 [build-system]
-requires = ["setuptools>=83.0.0", "wheel", "Cython>=3.0.0"]
+requires = ["setuptools", "wheel", "Cython>=3.0.0"]
 build-backend = "setuptools.build_meta"
 
 [tool.pytest.ini_options]
