--- CMakeLists.txt.orig	2026-02-27 11:01:34 UTC
+++ CMakeLists.txt
@@ -5,17 +5,14 @@ FIND_PACKAGE(ODBC REQUIRED)
 PROJECT(odbccpp)
 
 FIND_PACKAGE(ODBC REQUIRED)
-FIND_PACKAGE(Doxygen)
-FIND_PACKAGE(GTest)
 
 FILE(STRINGS cfg/VERSION version)
 
 ENABLE_TESTING()
 
 ADD_SUBDIRECTORY(src)
-IF (GTEST_FOUND)
+
+IF (BUILD_TESTING)
+    FIND_PACKAGE(GTest)
     ADD_SUBDIRECTORY(test)
-ENDIF()
-IF (DOXYGEN_FOUND)
-    ADD_SUBDIRECTORY(doc)
 ENDIF()
