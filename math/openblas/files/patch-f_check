--- f_check.orig	2023-04-01 20:18:01 UTC
+++ f_check
@@ -360,6 +360,16 @@ if [ -n "$link" ]; then
     	        flag="-lomp"
     	        ;;
     	    esac
+
+    	    case "$hostos" in "FreeBSD")
+        	    case "$CC" in
+                    gcc*) ;;
+                    *)
+                        # clang is assumed
+                        flag="-lomp"
+                        ;;
+                esac
+    	    esac
     	esac
 
         case "$flag" in -l*)
