-- Use the system Catch2 package instead of fetching it via CMake's FetchContent.
-- This avoids network access during the build/test phase and uses the FreeBSD
-- devel/catch port (Catch2 v2.x) which matches the upstream test expectations.

--- tests/CMakeLists.txt.orig	2025-03-21 15:13:09 UTC
+++ tests/CMakeLists.txt
@@ -35,13 +35,6 @@ set(CMAKE_CXX_STANDARD 14)
 # ----------------------------------------------
 # Catch2
 # ----------------------------------------------
-include(FetchContent)
-
-FetchContent_Declare(
-  Catch2
-  GIT_REPOSITORY https://github.com/catchorg/Catch2.git
-  GIT_TAG v2.13.6)
-FetchContent_MakeAvailable(Catch2)
-# Adds Catch2::Catch2
+find_package(Catch2 REQUIRED)

 # ----------------------------------------------
 # Test Inclusion
