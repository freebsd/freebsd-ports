PORTNAME=	zabbix6
PORTREVISION=	0
CATEGORIES=	net-mgmt
PKGNAMESUFFIX=	-agent

MASTERDIR=	${.CURDIR}/../${PORTNAME}-server
PLIST=		${PKGDIR}/pkg-plist.agent

OPTIONS_DEFINE=		CURL IPV6 MODBUS
OPTIONS_DEFAULT=	CURL OPENSSL

CPE_PRODUCT=	zabbix-agent

.include "${MASTERDIR}/Makefile"
