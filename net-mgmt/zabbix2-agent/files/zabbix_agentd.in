#!/bin/sh
# PROVIDE: zabbix_agentd
# REQUIRE: DAEMON
# KEYWORD: shutdown

. %%RC_SUBR%%

name="zabbix_agentd"
rcvar=${name}_enable
load_rc_config $name
eval : \${${rcvar}='NO'}

command="%%PREFIX%%/bin/${name}"
required_files="%%ETCDIR%%/${name}.conf"

run_rc_command "$1"
