#!/bin/sh
# PROVIDE: zabbix_server
# REQUIRE: DAEMON
# KEYWORD: shutdown

. %%RC_SUBR%%

name="zabbix_server"
rcvar=${name}_enable
load_rc_config $name

command="%%PREFIX%%/bin/${name}"
required_files="%%PREFIX%%/etc/zabbix/${name}.conf"

eval "${rcvar}=\${${rcvar}:-'NO'}"

run_rc_command "$1"
