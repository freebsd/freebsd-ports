#!/bin/sh

cd $WRKSRC || exit 1;

cat >> Makefile << END

BINDIR = $PREFIX/bin
CDDIR  = $PREFIX/lib/xcd
MANDIR = $PREFIX/man/man1

END

mv xcd.in xcd.in~
sed -e 's/wish4\.1/wish8.0/' <  xcd.in~ > xcd.in
exit 0;
