##
To view the README:

   cat ${PREFIX}/share/gnome/gdesklets/Sensors/CornerXMMS/README

To view the installed Displays:

   ls ${PREFIX}/share/gnome/gdesklets/Displays

To use those desklet:

   gdesklets open ${PREFIX}/share/gnome/gdesklets/Displays/CornerXMMS/cornerxmms-bottomright.display

NOTE: Be sure to login as your user when you use the gdesklets command,
      as the configuration will be stored in the ~/.gdesklets/ directory
      and/or gconf database.
##
