--- kscd/Makefile.in.orig	Wed Sep  8 00:23:41 1999
+++ kscd/Makefile.in	Fri Mar 10 01:01:13 2000
@@ -468,7 +468,7 @@
 	    test -d $(distdir)/$$subdir \
 	    || mkdir $(distdir)/$$subdir \
 	    || exit 1; \
-	    chmod 777 $(distdir)/$$subdir; \
+	    chmod 1777 $(distdir)/$$subdir; \
 	    (cd $$subdir && $(MAKE) $(AM_MAKEFLAGS) top_distdir=../$(top_distdir) distdir=../$(distdir)/$$subdir distdir) \
 	      || exit 1; \
 	  fi; \
@@ -563,7 +563,7 @@
 	for i in $(CDDB_DIRS); do \
 	if test ! -d $(CDDBDIR)/$$i; then \
 		$(mkinstalldirs) $(CDDBDIR)/$$i ;\
-		chmod 0777 $(CDDBDIR)/$$i ;\
+		chmod 1777 $(CDDBDIR)/$$i ;\
 	fi ;\
 	done 
 	$(mkinstalldirs) $(kde_icondir)
