# $FreeBSD$
#

    SUBDIR += imm
    SUBDIR += nte
    SUBDIR += rat
    SUBDIR += rqm
    SUBDIR += rtpmon
    SUBDIR += rtptools
    SUBDIR += rqm
    SUBDIR += sdr
    SUBDIR += speak_freely
    SUBDIR += vat
    SUBDIR += vic
    SUBDIR += wb

.include <bsd.port.subdir.mk>
