# $FreeBSD$
#

    SUBDIR += imm
    SUBDIR += mbone_vcr
    SUBDIR += nte
    SUBDIR += rat
    SUBDIR += rtpmon
    SUBDIR += rtptools
    SUBDIR += sdr
    SUBDIR += sdr-current
    SUBDIR += speak_freely
    SUBDIR += vat
    SUBDIR += vic
    SUBDIR += wb

.include <bsd.port.subdir.mk>
