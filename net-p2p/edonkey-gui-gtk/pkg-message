	ATTENTIONATTENTION:

This port is an attempt to distribute a FreeBSD native version of
the eDonkey GTK GUI written by Tim-Philipp Mller.

It might not work for all. If it does not work for you, try the
Linux version of this port available at
/usr/ports/net/linux-edonkey-gui-gtk. It might work better.

Currently, this native version is restricted to the following FreeBSD
platforms:

- i386 FreeBSD 4.x-STABLE (post 4.6R Release); OSVERSION=450006
