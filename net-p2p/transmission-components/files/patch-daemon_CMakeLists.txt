--- daemon/CMakeLists.txt.orig	2026-02-21 07:44:09 UTC
+++ daemon/CMakeLists.txt
@@ -33,7 +33,7 @@ target_link_libraries(${TR_NAME}-daemon
 target_link_libraries(${TR_NAME}-daemon
     PRIVATE
         ${TR_NAME}
-        libevent::event
+        libevent::core
         fmt::fmt-header-only
         $<$<BOOL:${WITH_SYSTEMD}>:${SYSTEMD_LIBRARIES}>)
 
