--- configure.orig	2010-01-25 12:47:28.000000000 +0100
+++ configure	2011-03-11 17:11:27.000000000 +0100
@@ -18550,7 +18550,6 @@
 
     fi
   fi
-     USE_NLS=yes
 
 
     gt_cv_have_gettext=no
@@ -21388,17 +21387,17 @@
 if test "x${prefix}" = "xNONE"; then
 
 cat >>confdefs.h <<_ACEOF
-#define PIXMAPS_DIR "${ac_default_prefix}/${DATADIRNAME}/${PACKAGE}/pixmaps"
+#define PIXMAPS_DIR "${ac_default_prefix}/${DATADIRNAME}/pixmaps"
 _ACEOF
 
-  pixmapsdir="${ac_default_prefix}/${DATADIRNAME}/${PACKAGE}/pixmaps"
+  pixmapsdir="${ac_default_prefix}/${DATADIRNAME}/pixmaps"
 else
 
 cat >>confdefs.h <<_ACEOF
-#define PIXMAPS_DIR "${prefix}/${DATADIRNAME}/${PACKAGE}/pixmaps"
+#define PIXMAPS_DIR "${prefix}/${DATADIRNAME}/pixmaps"
 _ACEOF
 
-  pixmapsdir="${prefix}/${DATADIRNAME}/${PACKAGE}/pixmaps"
+  pixmapsdir="${prefix}/${DATADIRNAME}/pixmaps"
 fi
 
 
