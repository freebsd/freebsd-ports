--- buildsys.mk.in.orig	2009-05-14 11:22:45.000000000 +0200
+++ buildsys.mk.in	2009-05-14 11:23:14.000000000 +0200
@@ -123,7 +123,7 @@
 	done; \
 	if test x"$$regen" = x"1" -a x"$$deps" != "x"; then \
 		${DEPEND_STATUS}; \
-		if ${MAKE} ${MFLAGS} $$deps && cat $$deps >.deps; then \
+		if ${MAKE} ${MFLAGS} $$deps && sed 's|/[^ ]*/include/audacious/dbus.h||g' $$deps >.deps; then \
 			rm -f $$deps; \
 			${DEPEND_OK}; \
 		else \
