--- Makefile.am.extra.orig	2008-04-21 14:54:10.224387369 +0200
+++ Makefile.am.extra	2008-04-21 14:55:36.076824474 +0200
@@ -128,10 +128,10 @@
 endif
 
 install-data-local: $(DEVPLUGINLIBSINSTALL) $(TVGUIDEDEVPLUGINLIBSINSTALL) 
-	mkdir -p $(DESTDIR)/$(libdir)/
-	OLDDIR=$(PWD)
-	cd $(DESTDIR)/$(libdir)/../ && $(LN_DIR) lib/nmm/dev-lib . && $(LN_DIR) share/nmm/ resources
-	cd $(OLDDIR)
+#	mkdir -p $(DESTDIR)/$(libdir)/
+#	OLDDIR=$(PWD)
+#	cd $(DESTDIR)/$(libdir)/../ && $(LN_DIR) lib/nmm/dev-lib . && $(LN_DIR) share/nmm/ resources
+#	cd $(OLDDIR)
  
 
 $(DEVPLUGINDIR)/%.so: %.la
