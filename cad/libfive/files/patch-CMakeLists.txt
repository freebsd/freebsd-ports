--- CMakeLists.txt.orig	2025-11-12 13:52:17 UTC
+++ CMakeLists.txt
@@ -64,7 +64,7 @@ if(NOT MSVC)
     endif()
 
     set(CMAKE_CXX_FLAGS_DEBUG "-O0")
-    set(CMAKE_CXX_FLAGS_RELEASE "${CMAKE_CXX_FLAGS_RELEASE} -O3 -DEIGEN_NO_DEBUG")
+    set(CMAKE_CXX_FLAGS_RELEASE "${CMAKE_CXX_FLAGS_RELEASE} -DEIGEN_NO_DEBUG")
 else()
     set(CMAKE_WINDOWS_EXPORT_ALL_SYMBOLS ON)
     set(CMAKE_CXX_FLAGS "/EHsc /WX /D_USE_MATH_DEFINES /D_SCL_SECURE_NO_WARNINGS")
@@ -124,7 +124,7 @@ if (UNIX AND NOT(APPLE))
 
 if (UNIX AND NOT(APPLE))
     find_package(Threads REQUIRED)
-    set(CMAKE_CXX_LINK_FLAGS "${CMAKE_CXX_LINK_FLAGS} -latomic")
+    # set(CMAKE_CXX_LINK_FLAGS "${CMAKE_CXX_LINK_FLAGS} -latomic")
 endif(UNIX AND NOT(APPLE))
 
 ################################################################################
