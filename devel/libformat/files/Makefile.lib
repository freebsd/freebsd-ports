PREFIX?=	/usr/local
SHLIB_VER?=	1

LIB=	format
LIBDIR=	${PREFIX}/lib
SHLIB_MAJOR=	${SHLIB_VER}
SHLIB_MINOR=	0
NOPROFILE=	true
NOOBJ=	true

INCS=	format.h
INCSDIR=	${PREFIX}/include
INCDIR=	${INCSDIR}			# for pre-bsd.incs.mk API

SRCS=	format.cpp

.include <bsd.lib.mk>
