--- Makefile.in.orig	2026-01-31 13:04:19 UTC
+++ Makefile.in
@@ -376,8 +376,8 @@ AM_DISTCHECK_CONFIGURE_FLAGS = --enable-cvg
 # whether the end-user will enable this; this step is here so that all the
 # relevant files are included in the distribution.
 AM_DISTCHECK_CONFIGURE_FLAGS = --enable-cvg
-SUBDIRS = compat gitwrapper got gotadmin gotctl gotd gotsh gotwebctl \
-	gotwebd libexec template tog $(am__append_1)
+SUBDIRS = compat got gotadmin \
+	libexec template tog $(am__append_1)
 EXTRA_DIST = CHANGES \
 	     CHANGELOG \
 	     LICENCE \
