--- third_party/webrtc/BUILD.gn.orig	2026-08-24 20:59:34 UTC
+++ third_party/webrtc/BUILD.gn
@@ -264,6 +264,9 @@ config("common_inherited_config") {
   if (is_linux || is_chromeos) {
     defines += [ "WEBRTC_LINUX" ]
   }
+  if (is_bsd) {
+    defines += [ "WEBRTC_BSD" ]
+  }
   if (is_mac) {
     defines += [ "WEBRTC_MAC" ]
   }
