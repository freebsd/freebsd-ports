--- pyproject.toml.orig	2025-12-15 22:19:06 UTC
+++ pyproject.toml
@@ -2,6 +2,6 @@ requires = [
 requires = [
     "cython>=3.1.0",
     "numpy>=2.0.0,<3",
-    "setuptools>=70.1.0",
+    "setuptools>=43.0.0",
 ]
 build-backend = "setuptools.build_meta"
