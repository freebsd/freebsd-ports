--- pyproject.toml.orig	2023-05-27 11:12:17 UTC
+++ pyproject.toml
@@ -1,6 +1,6 @@
 [build-system]
 requires = [
-    "setuptools >= 64",
+    "setuptools >= 61",
     "setuptools_scm >= 6.4"
 ]
 build-backend = "setuptools.build_meta"
