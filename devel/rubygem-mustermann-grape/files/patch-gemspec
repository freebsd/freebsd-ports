--- mustermann-grape.gemspec.orig	2019-12-30 16:24:50 UTC
+++ mustermann-grape.gemspec
@@ -23,7 +23,7 @@ Gem::Specification.new do |s|
     s.specification_version = 4
 
     if Gem::Version.new(Gem::VERSION) >= Gem::Version.new('1.2.0') then
-      s.add_runtime_dependency(%q<mustermann>.freeze, ["~> 1.0.0"])
+      s.add_runtime_dependency(%q<mustermann>.freeze, ["~> 1.0"])
     else
       s.add_dependency(%q<mustermann>.freeze, ["~> 1.0.0"])
     end
