--- pyproject.toml.orig	2024-08-28 18:03:20 UTC
+++ pyproject.toml
@@ -1,5 +1,5 @@
 [build-system]
-requires = ["setuptools>=72.2.0"]
+requires = ["setuptools>=61"]
 build-backend = "setuptools.build_meta"
 
 [project]
@@ -29,7 +29,7 @@ dependencies = [
 dynamic = ["version", "readme"]
 
 dependencies = [
-    "botocore >=1.35.0, <1.35.8", # NOTE: When updating, always keep `project.optional-dependencies` aligned
+    "botocore >=1.35.0", # NOTE: When updating, always keep `project.optional-dependencies` aligned
     "aiohttp >=3.9.2, <4.0.0",
     "wrapt >=1.10.10, <2.0.0",
     "aioitertools >=0.5.1, <1.0.0",
@@ -37,10 +37,10 @@ awscli = [
 
 [project.optional-dependencies]
 awscli = [
-    "awscli >=1.34.0, <1.34.8",
+    "awscli >=1.29.81",
 ]
 boto3 = [
-    "boto3 >=1.35.0, <1.35.8",
+    "boto3 >=1.35.0",
 ]
 
 [project.urls]
