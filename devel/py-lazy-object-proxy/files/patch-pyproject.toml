--- pyproject.toml.orig	2023-12-13 17:32:08 UTC
+++ pyproject.toml
@@ -1,6 +1,6 @@ requires = [
 [build-system]
 requires = [
-    "setuptools>=64",
+    "setuptools>=61",
     "setuptools_scm>=8",
 ]
 build-backend = "setuptools.build_meta"
