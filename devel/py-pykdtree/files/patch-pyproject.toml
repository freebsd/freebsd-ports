--- pyproject.toml.orig	2023-11-29 18:29:08 UTC
+++ pyproject.toml
@@ -1,3 +1,3 @@
 [build-system]
-requires = ["setuptools", "numpy", "Cython>=3"]
+requires = ["setuptools", "numpy", "Cython"]
 build-backend = "setuptools.build_meta"
