--- pyproject.toml.orig	2026-05-18 18:09:02 UTC
+++ pyproject.toml
@@ -1,5 +1,5 @@
 [build-system]
-requires = ["setuptools >= 80", "setuptools-scm >= 8.2"]
+requires = ["setuptools >= 61", "setuptools-scm >= 8.2"]
 build-backend = "setuptools.build_meta"
 
 [project]
@@ -13,8 +13,7 @@ readme = "README.rst"
     {name = "Thomas Kemmer", email = "tkemmer@computer.org"}
 ]
 readme = "README.rst"
-license = "MIT"
-license-files = ["LICENSE"]
+license = {text = "MIT"}
 requires-python = ">= 3.10"
 classifiers = [
     "Development Status :: 5 - Production/Stable",
