genanki is a library for generating Anki decks programmatically.
It allows you to create notes, cards, and decks with custom models
and templates, then export them as .apkg files that can be imported
into Anki.
