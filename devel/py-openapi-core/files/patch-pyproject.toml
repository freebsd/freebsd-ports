--- pyproject.toml.orig	1970-01-01 00:00:00 UTC
+++ pyproject.toml
@@ -67,11 +67,11 @@ more-itertools = "*"
 starlette = {version = ">=0.40.0,<1.1.0", optional = true}
 isodate = "*"
 more-itertools = "*"
-openapi-schema-validator = ">=0.7.0,<0.9.0"
-openapi-spec-validator = "^0.8.0"
+openapi-schema-validator = ">=0.7.0,<0.10.0"
+openapi-spec-validator = ">=0.8.0,<0.10"
 requests = {version = "*", optional = true}
 werkzeug = ">=2.1.0"
-jsonschema-path = "^0.4.5"
+jsonschema-path = ">=0.4.5,<0.6"
 jsonschema = "^4.23.0"
 multidict = {version = "^6.0.4", optional = true}
 aioitertools = {version = ">=0.11,<0.14", optional = true}
