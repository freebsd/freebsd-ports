Relax uv-build upper bound for devel/py-uv-build 0.12.0 compatibility.

--- pyproject.toml.orig	2026-07-29 00:00:00 UTC
+++ pyproject.toml
@@ -1,5 +1,5 @@
 [build-system]
-requires = ["uv_build>=0.11.19,<0.12.0"]
+requires = ["uv_build>=0.11.19"]
 build-backend = "uv_build"
 
 [tool.uv.build-backend]
