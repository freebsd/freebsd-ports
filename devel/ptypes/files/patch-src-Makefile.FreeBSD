--- src/Makefile.FreeBSD.orig	Sun Jan 25 20:37:18 2004
+++ src/Makefile.FreeBSD	Thu Jan 29 19:46:39 2004
@@ -15,10 +15,10 @@
 
 CXX = g++
 
-OS_CXXOPTS = -pthread
-OS_LDLIBS = -lc_r
+OS_CXXOPTS = $(PTHREAD_CFLAGS)
+OS_LDLIBS = $(PTHREAD_LIBS)
 
-LIBTOOL = $(CXX) -shared -pthread
+LIBTOOL = $(CXX) -shared $(PTHREAD_LIBS)
 SOSTRIP = strip
 
 SOEXT = so
