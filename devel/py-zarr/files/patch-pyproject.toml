--- pyproject.toml.orig	2024-12-12 16:02:37 UTC
+++ pyproject.toml
@@ -1,5 +1,5 @@
 [build-system]
-requires = ["setuptools>=64", "setuptools-scm>8"]
+requires = ["setuptools", "setuptools-scm>8"]
 build-backend = "setuptools.build_meta"
 
 
