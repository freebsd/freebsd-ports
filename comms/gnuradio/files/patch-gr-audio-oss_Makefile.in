--- gr-audio-oss/Makefile.in.orig	Thu Sep 13 10:00:34 2007
+++ gr-audio-oss/Makefile.in	Thu Sep 13 10:00:51 2007
@@ -408,12 +408,11 @@
 # Fix for BSD make not defining $(RM).  We define it now in configure.ac
 # using AM_PATH_PROG, but now here have to add a -f to be like GNU make
 RM = $(RM_PROG) -f
-EXTRA_DIST = \
-	gr-audio-oss.conf
+EXTRA_DIST = 
 
 SUBDIRS = src
 etcdir = $(sysconfdir)/gnuradio/conf.d
-etc_DATA = gr-audio-oss.conf
+etc_DATA =
 all: all-recursive
 
 .SUFFIXES:
