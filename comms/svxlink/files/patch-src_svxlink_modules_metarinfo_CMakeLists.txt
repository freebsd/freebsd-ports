--- src/svxlink/modules/metarinfo/CMakeLists.txt.orig	2026-05-23 14:59:55 UTC
+++ src/svxlink/modules/metarinfo/CMakeLists.txt
@@ -6,6 +6,7 @@ set(LIBS ${LIBS} ${CURL_LIBRARY})
 
 # Project libraries to link to
 set(LIBS ${LIBS} ${CURL_LIBRARY})
+include_directories(AFTER "${LOCALBASE}/include")
 
 # Build the plugin
 add_library(Module${MODNAME} MODULE Module${MODNAME}.cpp ${MODSRC})
