--- Makefile.orig	Mon Mar 27 22:38:37 2006
+++ Makefile	Mon Jun  5 15:37:15 2006
@@ -3,15 +3,16 @@
 
 
 
-CC	= gcc
-CFLAGS	= -O2 -g -Wall `pkg-config gtk+-2.0 --cflags`
+CC	?= gcc
+CFLAGS	+= `pkg-config gtk+-2.0 --cflags`
 
-LD	= $(CC)
-LDFLAGS	= `pkg-config gtk+-2.0 --libs` -lm
+LD	?= $(CC)
+LDFLAGS	+= `pkg-config gtk+-2.0 --libs` -lm
 
 ifeq ($(PNG),yes)
 CFLAGS	+= -DHAVE_LIBPNG
-LDFLAGS	+= -lpng
+CFLAGS	+= `pkg-config --cflags ${LOCALBASE}/libdata/pkgconfig/libpng14.pc`
+LDFLAGS    += `pkg-config --libs ${LOCALBASE}/libdata/pkgconfig/libpng14.pc`
 endif
 
 SRCS	= xnecview.c xwin.c parse_input.c parse_output.c draw.c draw_opaque.c freqplot.c postscript.c icon.xbm
