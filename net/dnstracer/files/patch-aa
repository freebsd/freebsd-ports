--- dnstracer.c.orig	Fri Jun  7 00:45:45 2002
+++ dnstracer.c	Fri Jun  7 00:45:26 2002
@@ -484,11 +484,11 @@
 	return retval;
 
     case ns_t_cname:
-	sprintf(retval,printablename(getname(session,&buffer),dots));
+	sprintf(retval,"%s",printablename(getname(session,&buffer),dots));
 	return retval;
 
     case ns_t_txt:
-	sprintf(retval,printablename(getname(session,&buffer),dots));
+	sprintf(retval,"%s",printablename(getname(session,&buffer),dots));
 	return retval;
 
     case ns_t_mx:
@@ -502,11 +502,11 @@
     }
 
     case ns_t_hinfo:
-	sprintf(retval,printablename(getname(session,&buffer),dots));
+	sprintf(retval,"%s",printablename(getname(session,&buffer),dots));
 	return retval;
 
     case ns_t_ns:
-	sprintf(retval,printablename(getname(session,&buffer),dots));
+	sprintf(retval,"%s",printablename(getname(session,&buffer),dots));
 	return retval;
 
     default: return "unknown";
