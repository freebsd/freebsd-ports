#!/bin/sh
# $FreeBSD$

# PROVIDE: mdnsd
# REQUIRE: DAEMON
# KEYWORD: FreeBSD shutdown

mdnsd_enable="NO"

. %%RC_SUBR%%

name=mdnsd
rcvar=`set_rcvar`
command="%%PREFIX%%/sbin/${name}"
pidfile="/var/run/${name}.pid"

load_rc_config $name

run_rc_command $*
