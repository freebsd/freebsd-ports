--- daemons/ipa-slapi-plugins/ipa-sidgen/Makefile.am.orig	2025-01-15 09:52:47 UTC
+++ daemons/ipa-slapi-plugins/ipa-sidgen/Makefile.am
@@ -56,3 +56,9 @@ EXTRA_DIST =			\
 EXTRA_DIST =			\
 	$(app_DATA)		\
 	$(NULL)
+
+# FreeBSD: resolve 389-ds SLAPI symbols from libslapd.
+libipa_sidgen_la_LIBADD += -L$(libdir)/dirsrv -lslapd $(LIBICONV)
+libipa_sidgen_la_LDFLAGS += -Wl,-rpath,$(libdir)/dirsrv
+libipa_sidgen_task_la_LIBADD += -L$(libdir)/dirsrv -lslapd $(LIBICONV)
+libipa_sidgen_task_la_LDFLAGS += -Wl,-rpath,$(libdir)/dirsrv
