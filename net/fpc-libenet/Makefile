# $FreeBSD$

CATEGORIES=	net lang
PKGNAMESUFFIX=	-libenet

COMMENT=	Free Pascal unit for libenet library

USES=		fpc
USE_FPC=	rtl-extra

MASTERDIR=	${.CURDIR}/../../lang/fpc
WRKUNITDIR=	${FPCSRCDIR}/packages/${PKGNAMESUFFIX:S/-//}
EXTRACTUNITDIR=	${WRKUNITDIR}

.include "${MASTERDIR}/Makefile"
