--- pyproject.toml.orig	2026-02-07 03:06:49 UTC
+++ pyproject.toml
@@ -20,7 +20,7 @@ include = ["blobfile*"]
 include = ["blobfile*"]
 
 [build-system]
-requires = ["setuptools>=69"]
+requires = ["setuptools>=61"]
 build-backend = "setuptools.build_meta"
 
 [tool.black]
