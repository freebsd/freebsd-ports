#!/bin/sh

# usage: $0 machine

if [ $# != 1 ]; then
  echo "usage: $0 machine"
  exit 1
fi

unset DISPLAY
script=/a/asami/portbuild/scripts/doupgrademach

scp -q -p $script $1:$script
ssh -n $1 $script
