PKGNAMESUFFIX=	-${BACKEND}
COMMENT=	GIO-styled async APIs for Flatpak (${BACKEND} backend)

BACKEND=	gtk4

USE_GNOME=	gtk40

TEST_BUILD_DEPENDS=	gjs:lang/gjs

MASTERDIR=	${.CURDIR}/../libportal

.include "${MASTERDIR}/Makefile"
