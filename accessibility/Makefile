# $FreeBSD$
#
     SUBDIR += at-spi
     SUBDIR += atk
     SUBDIR += dasher
     SUBDIR += gail
     SUBDIR += gnomemag
     SUBDIR += gnomespeech
     SUBDIR += gnopernicus
     SUBDIR += gok
     SUBDIR += kdeaccessibility
     SUBDIR += linux-atk

.include <bsd.port.subdir.mk>
