# $FreeBSD$

PROG=		ddup
SRCS=		ddup.c \
		ddup_functions.c \
		getopt.c \
		getopt1.c \
		parse_config.c \
		parse_option.c \
		update.c
CFLAGS+=	${DEBUG}
MAN1=		ddup.1
BINDIR=		${LOCALBASE}/sbin
MANDIR=		${LOCALBASE}/man/man

.include <bsd.prog.mk>

