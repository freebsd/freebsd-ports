--- filebeat/filebeat.yml.orig	2020-03-25 18:24:22 UTC
+++ filebeat/filebeat.yml
@@ -179,8 +179,8 @@ output.elasticsearch:
 processors:
   - add_host_metadata: ~
   - add_cloud_metadata: ~
-  - add_docker_metadata: ~
-  - add_kubernetes_metadata: ~
+#  - add_docker_metadata: ~
+#  - add_kubernetes_metadata: ~
 
 #================================ Logging =====================================
 
