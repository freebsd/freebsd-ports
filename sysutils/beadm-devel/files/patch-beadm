--- beadm.orig	2014-03-12 02:11:19.000000000 -0400
+++ beadm	2015-01-06 08:59:19.896613256 -0500
@@ -535,7 +535,7 @@
       | grep -E "^${POOL}/${BEDS}/${2}(/|$)" \
       | while read NAME
         do
-          zfs set canmount=on ${NAME}
+          zfs set canmount=noauto ${NAME}
           while __be_clone ${NAME}
           do
             zfs promote ${NAME}
@@ -737,7 +737,7 @@
                   continue
                   ;;
                 (*)
-                  MOUNTPOINT="/$( echo "${FS}" | sed s^"${POOL}/${BEDS}/${2}/"g )"
+                  MOUNTPOINT="/$( echo "${FS}" | sed s^"${POOL}/${BEDS}/${2}/"^^g )"
                   ;;
               esac
             fi
