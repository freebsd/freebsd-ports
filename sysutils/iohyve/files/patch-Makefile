--- Makefile.orig	2017-06-19 23:37:38 UTC
+++ Makefile
@@ -26,6 +26,7 @@ install:: all
 	$(INSTALL) -c -m $(BINMODE) ${.OBJDIR}/$(SCRIPTS) $(BINDIR)/
 	$(INSTALL) -c ${.OBJDIR}/lib/* $(FILESDIR)/
 	$(INSTALL) -c ${.OBJDIR}/rc.d/* $(RCDIR)/
-	$(INSTALL) -c $(MAN).gz $(MANDIR)/
+	$(INSTALL) -c $(MAN) $(MANDIR)/
+	gzip $(MANDIR)/$(MAN)
 
 .include <bsd.prog.mk>
