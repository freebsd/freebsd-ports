Index: src/sensors.c
diff -u src/sensors.c.orig src/sensors.c
--- src/sensors.c.orig	Wed Jan  2 06:48:02 2002
+++ src/sensors.c	Tue Feb 12 02:49:08 2002
@@ -228,7 +228,7 @@
 /* ====== System dependent interface ====================================== */
 
 /* ------- FreeBSD ------------------------------------------------------- */
-#if defined(__FreeBSD__)
+#if defined(__FreeBSD__) && defined(__i386__)
 #include <osreldate.h>
 #include <machine/cpufunc.h>
 #if __FreeBSD_version >= 300000
@@ -805,7 +805,8 @@
 
 
 /* ----- Other Systems -------------------------------------------------- */
-#if !defined(__FreeBSD__) && !defined(__linux__) && !defined(__NetBSD__)
+#if !(defined(__FreeBSD__) && defined(__i386__)) && \
+    !defined(__linux__) && !defined(__NetBSD__)
 
 #define	SENSORS_DIR		""
 
@@ -817,7 +818,7 @@
 static gboolean
 setup_sensor_interface(void)
 	{
-#if defined(__FreeBSD__)
+#if defined(__FreeBSD__) && defined(__i386__)
 	get_temperature = get_freebsd_sensor;
 	get_fan = get_freebsd_sensor;
 	get_voltage = get_freebsd_sensor;
