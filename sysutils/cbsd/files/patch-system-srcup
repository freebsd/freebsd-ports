--- system/srcup.bak	2016-04-24 17:03:05.000000000 +0300
+++ system/srcup	2016-07-12 13:14:50.165156000 +0300
@@ -43,7 +43,7 @@
 		SCM_URL="${scmbase}/releng/${ver}"
 	fi
 
-	[ "$ver" = "11.0" -o "${ver}" = "11" ] && SCM_URL="${scmbase}/head"
+	[ "$ver" = "12.0" -o "${ver}" = "12" ] && SCM_URL="${scmbase}/head"
 
 	return 0
 }
