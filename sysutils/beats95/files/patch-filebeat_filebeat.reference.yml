--- filebeat/filebeat.reference.yml.orig	2026-09-09 20:15:21 UTC
+++ filebeat/filebeat.reference.yml
@@ -1465,12 +1465,12 @@ - type: filestream
 #filebeat.config:
   #inputs:
     #enabled: false
-    #path: inputs.d/*.yml
+    #path: filebeat.inputs.d/*.yml
     #reload.enabled: true
     #reload.period: 10s
   #modules:
     #enabled: true
-    #path: modules.d/*.yml
+    #path: filebeat.modules.d/*.yml
     #reload.enabled: true
     #reload.period: 10s
 
@@ -2544,7 +2544,7 @@ output.elasticsearch:
 
 # The directory from where to read the dashboards. The default is the `kibana`
 # folder in the home path.
-#setup.dashboards.directory: ${path.home}/kibana
+#setup.dashboards.directory: %%DATADIR%%/filebeat/kibana
 
 # The URL from where to download the dashboard archive. It is used instead of
 # the directory if it has a value.
