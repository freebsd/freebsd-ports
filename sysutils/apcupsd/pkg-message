**************************************************
NOTE:

Change to /dev and create a softlink to usv from
the serial line the USV is connected to, e.g.

  cd /dev
  ln -s cuaa1 usv

**************************************************
