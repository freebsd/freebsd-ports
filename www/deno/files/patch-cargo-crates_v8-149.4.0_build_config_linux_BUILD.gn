--- cargo-crates/v8-149.4.0/build/config/linux/BUILD.gn.orig	2006-07-24 01:21:28 UTC
+++ cargo-crates/v8-149.4.0/build/config/linux/BUILD.gn
@@ -40,10 +40,6 @@ config("runtime_library") {
     defines = [ "OS_CHROMEOS" ]
   }
 
-  if ((!is_chromeos || default_toolchain != "//build/toolchain/cros:target") &&
-      (!use_custom_libcxx || current_cpu == "mipsel")) {
-    libs = [ "atomic" ]
-  }
 }
 
 config("libcap") {
