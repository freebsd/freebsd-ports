--- src/common/core/limit/confs/http/limitconn.conf.orig	2026-05-25 18:23:18 UTC
+++ src/common/core/limit/confs/http/limitconn.conf
@@ -1,17 +1,17 @@
 {%- if has_variable(all, "USE_LIMIT_CONN", "yes") -%}
 
-map $http2 $v1ip {
+map $server_protocol $v1ip {
 	""           $binary_remote_addr;
 	default      "";
 }
 
-map $http2 $v2ip {
+map $server_protocol $v2ip {
 	# This will be "h2" for HTTP/2 connections. Empty otherwise.
 	default $binary_remote_addr;
 	""      "";
 }
 
-map $http3 $v3ip {
+map $server_protocol $v3ip {
 	# This will be "h3" for HTTP/3 connections. Empty otherwise.
 	default $binary_remote_addr;
 	""      "";
