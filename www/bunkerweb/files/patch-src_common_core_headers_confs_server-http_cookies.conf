--- src/common/core/headers/confs/server-http/cookies.conf.orig	2026-03-13 17:01:33 UTC
+++ src/common/core/headers/confs/server-http/cookies.conf
@@ -1,5 +1,3 @@
-{%- for k, v in all.items() %}
-	{%- if k.startswith("COOKIE_FLAGS") and v != "" %}
-set_cookie_flag {{ v }};
-	{% endif %}
-{%- endfor %}
+# Cookie flag handling is disabled on FreeBSD because the
+# ngx_http_cookie_flag_filter_module module is not available in
+# the OpenResty package used by this port.
