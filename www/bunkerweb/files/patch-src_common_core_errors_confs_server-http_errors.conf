--- src/common/core/errors/confs/server-http/errors.conf.orig	2026-09-21 08:05:39 UTC
+++ src/common/core/errors/confs/server-http/errors.conf
@@ -12,7 +12,9 @@
 error_page {{ code }} @bwcustomerror{{ code }};
 location @bwcustomerror{{ code }} {
 	root {% if ROOT_FOLDER == "" %}/var/www/html/{% if MULTISITE == "yes" %}{{ SERVER_NAME.split(" ")[0] }}{% endif %}{% else %}{{ ROOT_FOLDER }}{% endif %};
+	{% if has_modsecurity %}
 	modsecurity off;
+	{% endif %}
 	auth_basic off;
 	rewrite ^ {{ page }} break;
 }
@@ -21,7 +23,9 @@
 {% endif %}
 location = {{ page }} {
 	root {% if ROOT_FOLDER == "" %}/var/www/html/{% if MULTISITE == "yes" %}{{ SERVER_NAME.split(" ")[0] }}{% endif %}{% else %}{{ ROOT_FOLDER }}{% endif %};
+	{% if has_modsecurity %}
 	modsecurity off;
+	{% endif %}
 	internal;
 	auth_basic off;
 }
@@ -42,9 +46,11 @@
 location {% if intercepted_error_code == "400" %}= /{% else %} @{% endif %}bwerror{{ intercepted_error_code }} {
 	auth_basic off;
 	internal;
+	{% if has_modsecurity %}
 	modsecurity off;
+	{% endif %}
 	default_type 'text/html';
-	root /usr/share/bunkerweb/core/errors/files;
+	root /usr/local/share/bunkerweb/common/core/errors/files;
 	content_by_lua_block {
 		local logger = require "bunkerweb.logger"
 		local cerrors = require "errors.errors"
