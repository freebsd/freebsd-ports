--- patches/envoy/0004-pgv.patch.orig	2026-04-24 03:57:04 UTC
+++ patches/envoy/0004-pgv.patch
@@ -19,3 +19,28 @@ +     ++unicode_len;
 +     str_char += char_len;
 +     byte_len -= char_len;
 +     ++unicode_len;
+diff --git a/api/bazel/repositories.bzl b/api/bazel/repositories.bzl
+index b7dc2f4dd9..ef63b42684 100644
+--- a/api/bazel/repositories.bzl
++++ b/api/bazel/repositories.bzl
+@@ -20,7 +20,10 @@ def api_dependencies():
+     external_http_archive(
+         name = "com_envoyproxy_protoc_gen_validate",
+         patch_args = ["-p1"],
+-        patches = ["@envoy_api//bazel:pgv.patch"],
++        patches = [
++            "@envoy_api//bazel:pgv.patch",
++            "//bazel:protoc_gen_validate_freebsd.patch",
++        ],
+     )
+     external_http_archive(
+         name = "com_google_googleapis",
+@@ -35,6 +38,8 @@ def api_dependencies():
+     )
+     external_http_archive(
+         name = "rules_buf",
++        patches = ["//bazel:rules_buf.patch"],
++        patch_args = ["-p1"],
+     )
+     external_http_archive(
+         name = "rules_proto",
