--- media/BUILD.gn.orig	2022-12-01 10:35:46 UTC
+++ media/BUILD.gn
@@ -78,6 +78,9 @@ config("media_config") {
       defines += [ "DLOPEN_PULSEAUDIO" ]
     }
   }
+  if (use_sndio) {
+    defines += [ "USE_SNDIO" ]
+  }
   if (use_cras) {
     defines += [ "USE_CRAS" ]
   }
