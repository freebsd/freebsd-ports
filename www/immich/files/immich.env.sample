# Connection settings for the Immich server.  This file is read by the
# rc script as root, so it may contain the database password.
DB_HOSTNAME=localhost
DB_USERNAME=immich
DB_DATABASE_NAME=immich
DB_PASSWORD=changeme
REDIS_HOSTNAME=localhost
#REDIS_PORT=6379
