# $FreeBSD$

CATEGORIES=	www
PKGNAMESUFFIX=	-web
PORTREVISION=	1

USES=		cpe
NO_ARCH=	yes
NO_BUILD=	yes
PORTDATA=	web

MASTERDIR=	${.CURDIR}/../../net-p2p/transmission-cli
DESCR=		${.CURDIR}/pkg-descr
PLIST=		${NONEXISTENT}

SLAVEPORT=	web

.include "${MASTERDIR}/Makefile"
