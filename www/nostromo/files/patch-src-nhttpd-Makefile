--- src/nhttpd/Makefile	2016-04-12 15:02:06.000000000 -0400
+++ src/nhttpd/Makefile	2016-04-13 12:29:49.861046000 -0400
@@ -13,8 +13,15 @@
-BINOWN=	root
+BINOWN?=	root
 BINMOD=	0555
-BINDIR=	/usr/local/sbin
+BINDIR=	${PREFIX}/sbin
 
 OS!= uname -s
 
+.if defined(OPENSSLLIB) && "${OPENSSLLIB}" != "/usr/lib"
+LDADD+=	-L${OPENSSLIB}
+.endif
+.if defined(OPENSSLINC) && "${OPENSSLINC}" != "/usr/include"
+CFLAGS+=-I${OPENSSLINC}
+.endif
+
 LDADD+= -lssl -lcrypto
 .if ${OS} == "FreeBSD"
