--- Makefile.orig	2014-07-02 18:12:33.669901208 +0800
+++ Makefile	2014-07-02 18:13:37.501883432 +0800
@@ -52,18 +52,17 @@ ephemera.css.sample: ephemera.css
 
 .PHONY: install
 install: all
-	${INSTALL_SCRIPT} ephemera ${DEST}
+	${INSTALL_SCRIPT} ephemera $(DESTDIR)${DEST}
 	${GZIP_CMD} ephemera.8
-	${INSTALL_MAN} ephemera.8.gz ${MANDIR}
-	${INSTALL_DATA} ephemera.css.sample ${ETCDIR}
-	${INSTALL_DATA} ephemera.config.dist ${ETCDIR}	
-	if [ ! -e ${ETCDIR}/ephemera.config ]; then ${INSTALL_DATA} ephemera.config ${ETCDIR}; fi
-	${INSTALL_SCRIPT} ephemera.rc ${ETCDIR}/rc.d/ephemera
-	${INSTALL_SCRIPT} ephemera-add ${DEST}
-	${INSTALL_SCRIPT} ephemera-replace ${DEST}
-	${INSTALL_SCRIPT} ephemera-extract ${DEST}
-	${INSTALL_SCRIPT} ephemera-delete ${DEST}
-	${INSTALL_SCRIPT} ephemera-generate ${DEST}
+	${INSTALL_MAN} ephemera.8.gz $(DESTDIR)${MANDIR}
+	${INSTALL_DATA} ephemera.css.sample $(DESTDIR)${ETCDIR}
+	${INSTALL_DATA} ephemera.config.dist $(DESTDIR)${ETCDIR}/ephemera.config.sample
+	${INSTALL_SCRIPT} ephemera.rc $(DESTDIR)${ETCDIR}/rc.d/ephemera
+	${INSTALL_SCRIPT} ephemera-add $(DESTDIR)${DEST}
+	${INSTALL_SCRIPT} ephemera-replace $(DESTDIR)${DEST}
+	${INSTALL_SCRIPT} ephemera-extract $(DESTDIR)${DEST}
+	${INSTALL_SCRIPT} ephemera-delete $(DESTDIR)${DEST}
+	${INSTALL_SCRIPT} ephemera-generate $(DESTDIR)${DEST}
 
 .PHONY: deinstall
 deinstall:
