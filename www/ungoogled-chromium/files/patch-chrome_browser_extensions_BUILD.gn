--- chrome/browser/extensions/BUILD.gn.orig	2026-08-13 07:41:05 UTC
+++ chrome/browser/extensions/BUILD.gn
@@ -1525,6 +1525,10 @@ source_set("extensions") {
       ]
     }
 
+    if (is_bsd) {
+      sources -= [ "api/image_writer_private/removable_storage_provider_linux.cc" ]
+    }
+
     if (enable_pdf) {
       deps += [
         "//chrome/browser/pdf",
