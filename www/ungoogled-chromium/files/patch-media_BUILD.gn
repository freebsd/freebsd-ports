--- media/BUILD.gn.orig	2023-01-13 08:56:02 UTC
+++ media/BUILD.gn
@@ -88,6 +88,9 @@ config("media_config") {
       defines += [ "DLOPEN_PULSEAUDIO" ]
     }
   }
+  if (use_sndio) {
+    defines += [ "USE_SNDIO" ]
+  }
   if (use_cras) {
     defines += [ "USE_CRAS" ]
   }
