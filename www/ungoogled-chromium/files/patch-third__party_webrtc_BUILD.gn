--- third_party/webrtc/BUILD.gn.orig	2026-06-05 13:45:06 UTC
+++ third_party/webrtc/BUILD.gn
@@ -242,6 +242,9 @@ config("common_inherited_config") {
   if (is_linux || is_chromeos) {
     defines += [ "WEBRTC_LINUX" ]
   }
+  if (is_bsd) {
+    defines += [ "WEBRTC_BSD" ]
+  }
   if (is_mac) {
     defines += [ "WEBRTC_MAC" ]
   }
