--- third_party/webrtc/BUILD.gn.orig	2025-12-06 13:30:52 UTC
+++ third_party/webrtc/BUILD.gn
@@ -221,6 +221,9 @@ config("common_inherited_config") {
   if (is_linux || is_chromeos) {
     defines += [ "WEBRTC_LINUX" ]
   }
+  if (is_bsd) {
+    defines += [ "WEBRTC_BSD" ]
+  }
   if (is_mac) {
     defines += [ "WEBRTC_MAC" ]
   }
