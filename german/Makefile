# $FreeBSD$
#

    SUBDIR += BBBike
    SUBDIR += cheapcall
    SUBDIR += dict
    SUBDIR += manpages
    SUBDIR += phone
    SUBDIR += spinne
    SUBDIR += staroffice5
    SUBDIR += webalizer

.include <bsd.port.subdir.mk>
