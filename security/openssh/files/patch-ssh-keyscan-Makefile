--- ssh-keyscan/Makefile.orig	Sun Mar  4 08:59:39 2001
+++ ssh-keyscan/Makefile	Thu Jul 12 18:57:39 2001
@@ -7,12 +7,12 @@
 
 BINMODE?=555
 
-BINDIR=	/usr/bin
-MAN=	ssh-keyscan.1
+BINDIR=	/bin
+MAN1=	ssh-keyscan.1
 
 SRCS=	ssh-keyscan.c
 
 .include <bsd.prog.mk>
 
-LDADD+=	-lcrypto
+LDADD+=	${CRYPTOLIBS}
 DPADD+=	${LIBCRYPTO}
