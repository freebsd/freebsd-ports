--- Cargo.toml.orig	2023-11-27 22:02:02.951242432 +0100
+++ Cargo.toml	2023-11-27 22:02:25.455387731 +0100
@@ -139,10 +139,10 @@
 cookie_store = "0.19.1"
 
 # Used by U2F, JWT and PostgreSQL
-openssl = "=0.10.57"
+openssl = "=0.10.60"
 # Set openssl-sys fixed to v0.9.92 to prevent building issues with musl, arm and 32bit pointer width
 # It will force add a dynamically linked library which prevents the build from being static
-openssl-sys = "=0.9.92"
+openssl-sys = "=0.9.96"
 
 # CLI argument parsing
 pico-args = "0.5.0"
