--- crates/tor-proto/Cargo.toml.orig	2022-02-05 18:43:14 UTC
+++ crates/tor-proto/Cargo.toml
@@ -49,6 +49,6 @@ tokio-util = { version = "0.6", features = ["compat"],
 coarsetime = { version = "0.1.20", optional = true }
 
 [dev-dependencies]
-tor-rtcompat = { path = "../tor-rtcompat", version = "0.0.4", features = [ "tokio", "native-tls" ] }
+tor-rtcompat = { path = "../tor-rtcompat", version = "0.0.4", features = [ "tokio", "rustls" ] }
 hex-literal = "0.3"
 hex = "0.4"
