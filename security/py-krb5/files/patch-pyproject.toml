--- pyproject.toml.orig	2026-08-24 03:00:49 UTC
+++ pyproject.toml
@@ -1,6 +1,6 @@ requires = [
 [build-system]
 requires = [
-    "Cython == 3.3.0",
+    "Cython >= 3.2.9",
     "setuptools >= 42.0.0",  # Supports license_files
 ]
 build-backend = "setuptools.build_meta"
