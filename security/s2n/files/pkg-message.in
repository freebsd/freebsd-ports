======================================================================

If s2n fails due mlock(2) syscall try to increase vm.max_wired.
Example:
 - sysctl vm.max_wired=1572864

======================================================================
