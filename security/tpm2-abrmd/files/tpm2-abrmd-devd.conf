# Allow members of _tss group to access tpm device

notify 100 {
 match "system" "DEVFS";
 match "subsystem" "CDEV";
 match "type" "CREATE";
 match "cdev" "tpm0";
 action "/usr/sbin/chown root:_tss /dev/$cdev; /bin/chmod 660 /dev/$cdev";
};
