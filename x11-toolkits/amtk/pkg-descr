The Good Morning Toolkit is a library that extend GTK at the
exception of GtkTextView which is extended by libgedit-gtksourceview.
It provides a basic GtkUIManager replacement based on GAction that is
suitable for both a traditional UI or a modern UI with a GtkHeaderBar.
