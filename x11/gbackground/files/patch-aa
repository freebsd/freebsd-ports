--- Makefile.in.orig	Wed Jan 10 00:38:43 2001
+++ Makefile.in	Sat Jan 25 04:30:45 2003
@@ -1,5 +1,5 @@
-CC = gcc
-CFLAGS = -Wall -O2 -g `gnome-config --cflags applets`
+CC = @CC@
+CFLAGS = @CFLAGS@ `gnome-config --cflags applets`
 LFLAGS = `gnome-config --libs applets` -lm
 REL = `cat ./releasenum`
 SUMMARY = Gnome applet to change your background.
@@ -14,6 +14,10 @@
 DESKTOPDIR = `gnome-config --datadir`/applets/Amusements/
 DATADIR = `gnome-config --datadir`
 
+INSTALL = @INSTALL@
+INSTALL_PROGRAM = @INSTALL_PROGRAM@
+INSTALL_DATA = @INSTALL_DATA@
+
 SRC = gBackGround.c\
 	config_stuff.c\
 	callbacks.c\
@@ -44,12 +48,10 @@
 	$(CC) -o $(PTARGET) $(POBJS) $(LFLAGS)
 
 install:	$(TARGET) $(TARGET).gnorba $(TARGET).desktop $(PTARGET)
-	strip -sv $(TARGET)
-	strip -sv $(PTARGET)
-	/usr/bin/install $(TARGET) $(BINDIR)
-	/usr/bin/install $(TARGET).gnorba $(GNORBADIR)
-	/usr/bin/install $(TARGET).desktop $(DESKTOPDIR)
-	/usr/bin/install $(PTARGET) $(BINDIR)
+	${INSTALL_PROGRAM} $(TARGET) $(BINDIR)
+	${INSTALL_DATA} $(TARGET).gnorba $(GNORBADIR)
+	${INSTALL_DATA} $(TARGET).desktop $(DESKTOPDIR)
+	${INSTALL_DATA} $(PTARGET) $(BINDIR)
 
 uninstall:
 	rm -f $(BINDIR)/$(TARGET)
