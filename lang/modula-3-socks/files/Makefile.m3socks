CLEANFILES+=	m3socks
MAN1=		m3socks.1
MAN5=		m3socks.conf.5
BINDIR=		${PREFIX}/bin
MANDIR=		${PREFIX}/man/man

all:	m3socks

.SUFFIXES: .sh

.sh:
	cp $@.sh $@

realinstall:
	${INSTALL} ${COPY} -o ${BINOWN} -g ${BINGRP} -m ${BINMODE} \
	    m3socks ${BINDIR}

.include <bsd.prog.mk>
