#!/bin/sh
# $FreeBSD$

GREP=/usr/bin/grep
SED=/usr/bin/sed
MODE=default

MKTEXLSR=%%MKTEXLSR%%
TETEX_MODESW=%%TETEX_MODESW%%

case $2 in
POST-INSTALL)
	${TETEX_MODESW} ${MODE} dvips force
	${MKTEXLSR}
	;;
DEINSTALL)
	${TETEX_MODESW} uninstall dvips
	${MKTEXLSR}
	;;
esac
