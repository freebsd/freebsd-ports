#!/bin/sh

REPLACES_BINARIES="/bin/lpr
					/bin/lp
					/bin/lpq
					/bin/lprm
					/bin/lpstat
					/sbin/lpc"

case $2 in
POST-INSTALL)
	for FILE in ${REPLACES_BINARIES}; do
		[ -e "${FILE}" ] && mv "${FILE}" "${FILE}.bak"
	done
;;
esac
exit 0
